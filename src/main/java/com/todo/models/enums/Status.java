package com.todo.models.enums;

// Enum representing the status of a todo item
public enum Status {
    // Status indicating that the todo item is completed
    completed,
    // Status indicating that the todo item is cancelled
    cancelled,
    // Status indicating that the todo item is incomplete
    incomplete
}
